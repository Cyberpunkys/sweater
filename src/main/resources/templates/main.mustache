<html lang="ru">

<body>
    <div>
        <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"> <!-- Любой запрос без токена будет игнорирован -->
            <input type="submit" value="Sign Out"/>
        </form>
    </div>

    <div>
        <form method="post"> <!-- Ввод данных в БД -->
            <input type="text" name="text" placeholder="Введите сообщение">
            <input type="text" name="tag" placeholder="Тэг">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">Добавить</button>
        </form>
    </div>

    <div>Список сообщений</div> <!-- Поиск сообшений в БД по тегу -->
    <form method="post" action="filter">
        <input type="text" name="filter">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <button type="submit">Найти</button>
    </form>

    {{#messages}} <!-- список сообщений в mustache -->
        <div>
            <b>{{id}}</b>
            <span>{{text}}</span>
            <i>#{{tag}}</i>
        </div>
    {{/messages}}

</body>
</html>
